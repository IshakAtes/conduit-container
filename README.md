# conduit-container
Containerized Conduit full-stack app with frontend &amp; backend, ready for cloud deployment


1. ssh einloggen
ssh -i ~/.ssh/<server-key-name> user@ip_adress

2. git clone <git-repo> conduit full-stack
git clone conduit frontend
git clone conduit backend

3. create .env file
cp example.env .env


4. docker compose up
aktuell dauert es zu lange. ?????